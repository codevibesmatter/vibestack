import { createFileRoute } from '@tanstack/react-router';

// This file is just a placeholder that will be replaced by the lazy-loaded route
export const Route = createFileRoute('/admin')({
  // The component is defined in admin.lazy.tsx
}) 