# Database connection URL (overrides individual settings if provided)
DATABASE_URL=postgresql://neondb_owner:PLACEHOLDER_PASSWORD@ep-project-name-region-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require

# Individual database settings (used if DATABASE_URL is not provided)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=PLACEHOLDER_DB_PASSWORD
DB_NAME=vibestack
DB_SSL=false

# Debug mode for database operations
DEBUG=false 